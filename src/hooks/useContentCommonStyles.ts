import makeStyles from '@material-ui/core/styles/makeStyles'

const useContentCommonStyles = makeStyles({
  root: {
    display: 'flex',
    marginBottom: 120
  }
})

export default useContentCommonStyles
